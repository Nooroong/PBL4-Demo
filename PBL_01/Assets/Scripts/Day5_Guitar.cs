using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Day5_Guitar : MonoBehaviour
{
    // Start is called before the first frame update
    void Awake()
    {
        PlayerPrefs.SetInt("Guitar", 1);
    }
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
